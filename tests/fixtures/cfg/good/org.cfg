# Clean organization-level configuration.
%level Organization

_USTN_WORKSPACEROOT : $(TESTROOT)\
MS_DGNLIBLIST > $(_USTN_WORKSPACEROOT)dgnlib\*.dgnlib
_CIVIL_STANDARDS_IMPORTEXPORT = 1

# A conditional set must not override the value already established above.
_USTN_WORKSPACEROOT : C:\Should\Never\Win\

%include shared.cfg

%if defined(_USTN_WORKSPACEROOT)
MS_SEEDFILES = $(_USTN_WORKSPACEROOT)seed\
%endif

%ifndef NOT_DEFINED_ANYWHERE
MS_PENTABLE = $(_USTN_WORKSPACEROOT)tables\
%endif
